import React from 'react';
import ReactDOM from 'react-dom/client';
import TechEquityAnalyzer from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TechEquityAnalyzer />
  </React.StrictMode>
);